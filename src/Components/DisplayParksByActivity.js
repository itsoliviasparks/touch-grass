// take usersState from StateSelector component

const DisplayParksByActivity = (parkListByActivity) => {
    return(
        console.log(parkListByActivity)
    );
};

export default DisplayParksByActivity;
